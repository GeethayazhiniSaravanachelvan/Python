import datetime

count = 0

for day in range(1,32):
    try:
        d = datetime.date(2025,1,day)
        if d.weekday() == 6:
            count += 1
    except:
        pass

print(count)